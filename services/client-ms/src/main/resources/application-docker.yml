spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/client_db?currentSchema=${CURRENT_SCHEMA}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: ${CURRENT_SCHEMA}
